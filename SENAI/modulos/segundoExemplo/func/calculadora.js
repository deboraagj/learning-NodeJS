const somarFunc = (x,y) => {
    let soma = x+y;
    return soma;
}

const multiplicarFunc = (x,y) => {
    let soma = x*y;
    return soma;
}

const dividirFunc = (x,y) => {
    let soma = x/y;
    return soma;
}

module.exports = {
    somarFunc,
    multiplicarFunc,
    dividirFunc
}