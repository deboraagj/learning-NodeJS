const diviFunc = (x,y) => {
    let soma = x / y;
    return soma;
}

const multiFunc = (x,y) => {
    let soma = x * y;
    return soma;
}

const somarFunc = (x,y) => {
    let soma = x + y;
    return soma;
}

module.exports = {
    somarFunc,
    multiFunc,
    diviFunc
}

